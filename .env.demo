# Loaded automatically by `npm run demo` (vite --mode demo).
#
# Runs the CRM against in-memory sample data with no PocketBase and no login.
# Safe to commit: it contains no secrets, and demo mode is gated on the dev
# server as well as this flag, so a production build ignores it entirely.
VITE_DEMO=true
